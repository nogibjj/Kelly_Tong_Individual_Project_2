```sql
SELECT * FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
CREATE TABLE IF NOT EXISTS AutoDB (MPG REAL, Cylinders INTEGER,Displacement REAL,Horsepower REAL,Weight REAL,Acceleration REAL,Year INTEGER,Origin INTEGER,Name Text);
```

```sql
INSERT INTO AutoDB (MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Year, Origin, Name) VALUES (25.0, 4, 150.0, 100.0, 2500.0, 10.0, 70, 1,'Example Auto');
```

```sql
SELECT * FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
UPDATE AutoDB SET MPG=20.0, Cylinders=6 WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
DELETE FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
SELECT * FROM AutoDB LIMIT 5;
```

```sql
SELECT * FROM AutoDB LIMIT 5;
```

```sql
CREATE TABLE IF NOT EXISTS AutoDB (MPG REAL, Cylinders INTEGER,Displacement REAL,Horsepower REAL,Weight REAL,Acceleration REAL,Year INTEGER,Origin INTEGER,Name Text);
```

```sql
INSERT INTO AutoDB (MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Year, Origin, Name) VALUES (25.0, 4, 150.0, 100.0, 2500.0, 10.0, 70, 1,'Example Auto');
```

```sql
INSERT INTO AutoDB (MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Year, Origin, Name) VALUES (25.0, 4, 150.0, 100.0, 2500.0, 10.0, 70, 1,'Example Auto');
```

```sql
SELECT * FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
UPDATE AutoDB SET MPG=20.0, Cylinders=6 WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
DELETE FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
SELECT * FROM AutoDB LIMIT 5;
```

```sql
CREATE TABLE IF NOT EXISTS AutoDB (MPG REAL, Cylinders INTEGER,Displacement REAL,Horsepower REAL,Weight REAL,Acceleration REAL,Year INTEGER,Origin INTEGER,Name Text);
```

```sql
INSERT INTO AutoDB (MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Year, Origin, Name) VALUES (25.0, 4, 150.0, 100.0, 2500.0, 10.0, 70, 1,'Example Auto');
```

```sql
INSERT INTO AutoDB (MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Year, Origin, Name) VALUES (25.0, 4, 150.0, 100.0, 2500.0, 10.0, 70, 1,'Example Auto');
```

```sql
SELECT * FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
UPDATE AutoDB SET MPG=20.0, Cylinders=6 WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
DELETE FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
SELECT * FROM AutoDB LIMIT 5;
```

```sql
CREATE TABLE IF NOT EXISTS AutoDB (MPG REAL, Cylinders INTEGER,Displacement REAL,Horsepower REAL,Weight REAL,Acceleration REAL,Year INTEGER,Origin INTEGER,Name Text);
```

```sql
INSERT INTO AutoDB (MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Year, Origin, Name) VALUES (25.0, 4, 150.0, 100.0, 2500.0, 10.0, 70, 1,'Example Auto');
```

```sql
INSERT INTO AutoDB (MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Year, Origin, Name) VALUES (25.0, 4, 150.0, 100.0, 2500.0, 10.0, 70, 1,'Example Auto');
```

```sql
SELECT * FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
UPDATE AutoDB SET MPG=20.0, Cylinders=6 WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
DELETE FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
SELECT * FROM AutoDB LIMIT 5;
```

```sql
CREATE TABLE IF NOT EXISTS AutoDB (MPG REAL, Cylinders INTEGER,Displacement REAL,Horsepower REAL,Weight REAL,Acceleration REAL,Year INTEGER,Origin INTEGER,Name Text);
```

```sql
INSERT INTO AutoDB (MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Year, Origin, Name) VALUES (25.0, 4, 150.0, 100.0, 2500.0, 10.0, 70, 1,'Example Auto');
```

```sql
INSERT INTO AutoDB (MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Year, Origin, Name) VALUES (25.0, 4, 150.0, 100.0, 2500.0, 10.0, 70, 1,'Example Auto');
```

```sql
SELECT * FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
UPDATE AutoDB SET MPG=20.0, Cylinders=6 WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
DELETE FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
SELECT * FROM AutoDB LIMIT 5;
```

```sql
CREATE TABLE IF NOT EXISTS AutoDB (MPG REAL, Cylinders INTEGER,Displacement REAL,Horsepower REAL,Weight REAL,Acceleration REAL,Year INTEGER,Origin INTEGER,Name Text);
```

```sql
INSERT INTO AutoDB (MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Year, Origin, Name) VALUES (25.0, 4, 150.0, 100.0, 2500.0, 10.0, 70, 1,'Example Auto');
```

```sql
INSERT INTO AutoDB (MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Year, Origin, Name) VALUES (25.0, 4, 150.0, 100.0, 2500.0, 10.0, 70, 1,'Example Auto');
```

```sql
SELECT * FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
UPDATE AutoDB SET MPG=20.0, Cylinders=6 WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
DELETE FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
SELECT * FROM AutoDB LIMIT 5;
```

```sql
CREATE TABLE IF NOT EXISTS AutoDB (MPG REAL, Cylinders INTEGER,Displacement REAL,Horsepower REAL,Weight REAL,Acceleration REAL,Year INTEGER,Origin INTEGER,Name Text);
```

```sql
INSERT INTO AutoDB (MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Year, Origin, Name) VALUES (25.0, 4, 150.0, 100.0, 2500.0, 10.0, 70, 1,'Example Auto');
```

```sql
INSERT INTO AutoDB (MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Year, Origin, Name) VALUES (25.0, 4, 150.0, 100.0, 2500.0, 10.0, 70, 1,'Example Auto');
```

```sql
SELECT * FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
UPDATE AutoDB SET MPG=20.0, Cylinders=6 WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
DELETE FROM AutoDB WHERE Name = 'Chevrolet Chevelle Malibu';
```

```sql
SELECT * FROM AutoDB LIMIT 5;
```

